{
  "name": "@monorepo/root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna exec --parallel -- babel --root-mode upward src -d lib --ignore **/*.stories.js,**/*.spec.js"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
